version: 0.2
env:
  git-credential-helper: yes

phases:
  install:
    runtime-versions:
      docker: 18
      # nodejs: 16
    commands:
      - pip3 install --upgrade aws-sam-cli
  pre_build:
    commands:

      # - | 
      #   if [ "$PR_EVENT" == "closed" ]; then
      #     echo "PR was closed, cleaning up resources...";
      #     aws cloudformation delete-stack --stack-name ${APP_NAME}-ephemeral --output text || echo "Stack ${APP_NAME}-ephemeral deletion failed (stack may not exist), continuing...";
      #     aws cloudformation delete-stack --stack-name ${APP_NAME}-ecr --output text || echo "Stack ${APP_NAME}-ecr deletion failed (stack may not exist), continuing...";
      #     aws cloudformation delete-stack --stack-name ${APP_NAME}-pipeline --output text || echo "Stack ${APP_NAME}-ephemeral deletion failed (stack may not exist), continuing...";
      #     echo "Exiting build process to prevent further execution.";
      #     exit 0;
      #   fi


      - echo $CODEBUILD_RESOLVED_SOURCE_VERSION
      # - GLOBAL_TZ='PDT\|PST'
      # - echo Logging in to Amazon ECR...
      # - $(aws ecr get-login --no-include-email --region $AWS_DEFAULT_REGION)
      # - COMMIT_HASH=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c 1-7)
      # - IMAGE_TAG=${COMMIT_HASH:-latest}

      - WEBSITE_REPOSITORY_URI=$ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/website-$APP_NAME
      - TOPHAT_REPOSITORY_URI=$ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/tophat-$APP_NAME
      - API_SERVER_REPOSITORY_URI=$ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/apiserver-$APP_NAME

      # - NODE_PATH=$(pwd)/mr_modules
      - NODE_ENV=ecs




      - echo Listing Source Files...
      # list all files by most recently modified in reverse order
      - ls -ltra



      # - echo Checking out to default 'HEAD' branch
      # - git checkout $(git symbolic-ref refs/remotes/origin/HEAD | sed 's,refs/remotes/origin/,,1')  
      # - echo Listing master branch Source Files...

      # This will clone the main branch of color advisor (this works as long as the codestar connection has access to this repo)
      - git clone https://github.com/MadisonReed/color-advisor-microservice.git
      - cd color-advisor-microservice
      - echo listing color advisor microservice files...
      - ls -ltra
      - cd ../


  build:
    commands:
      - echo Build started on `date`

        # Deploy ECR repos
      - sam build -t ecr.yml
      - |
        sam deploy -t ecr.yml \
          --config-env ecr \
          --parameter-overrides \
            "pAppName=${APP_NAME}" \
          --stack-name ${APP_NAME}-ecr \
          --tags CleanupDate=$(date -u -d "+10 days" '+%Y-%m-%dT%H:%M:%SZ') \
          --no-confirm-changeset
          --on-failure DELETE

      # - echo node $(node -v) \(npm $(npm -v)\)
      # - bin/prod-node-modules.sh -t build
      # - bin/generateSecureConfig.js -e $NODE_ENV --microservice || true # TODO: no fail for now
      # - bin/downloadGeoIpDBFromS3.sh # needed for apiserver?
      # - bin/generateCodebaseJson # needed for apiserver?
      # - npm run gulp-build-website > gulpBuildWebsite.log 2>&1
      # - cd website
      # - npm ci > npmInstall.log 2>&1
      # - npm run buildProduction > buildProduction.log 2>&1
      # - npm run postBuildProduction > postBuildProduction.log 2>&1 || true # TODO: no fail for now
      # - cd ../


      - echo Building the Docker images...


      # build TOPHAT, WEBSITE, API SERVER images
      - echo Building Website...
      # # WEBSITE
      # - |
      #   docker build -f docker/website/Dockerfile -t $WEBSITE_REPOSITORY_URI:$IMAGE_TAG . \
      #     --build-arg AWS_DEFAULT_REGION=$AWS_DEFAULT_REGION \
      #     --build-arg ACCOUNT_ID=$ACCOUNT_ID \
      #     --build-arg BASE_IMAGE_REPO_NAME=$BASE_IMAGE_REPO_NAME \
      #     --build-arg BASE_IMAGE_TAG=$BASE_IMAGE_TAG \
      #     --build-arg NODE_ENV=$NODE_ENV \
      - echo Building API Server...
      # # API SERVER
      # - |
      #   docker build -f docker/apiserver/Dockerfile -t $API_SERVER_REPOSITORY_URI:$IMAGE_TAG . \
      #     --build-arg AWS_DEFAULT_REGION=$AWS_DEFAULT_REGION \
      #     --build-arg ACCOUNT_ID=$ACCOUNT_ID \
      #     --build-arg BASE_IMAGE_REPO_NAME=$BASE_IMAGE_REPO_NAME \
      #     --build-arg BASE_IMAGE_TAG=$BASE_IMAGE_TAG \
      #     --build-arg NODE_ENV=$NODE_ENV \
      - echo Building Tophat...
      # # TOPHAT
      # - |
      #   docker build -f docker/apiserver/Dockerfile -t $TOPHAT_REPOSITORY_URI:$IMAGE_TAG . \
      #     --build-arg AWS_DEFAULT_REGION=$AWS_DEFAULT_REGION \
      #     --build-arg ACCOUNT_ID=$ACCOUNT_ID \
      #     --build-arg BASE_IMAGE_REPO_NAME=$BASE_IMAGE_REPO_NAME \
      #     --build-arg BASE_IMAGE_TAG=$BASE_IMAGE_TAG \
      #     --build-arg NODE_ENV=$NODE_ENV \

      # prepare nested stack
      # - sam build -t root.yml

  # post_build:
  #   commands:
  #     - echo Build completed on `date`
  #     - echo Pushing Website Docker image...
  #     - docker push $ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$WEBSITE_REPOSITORY_URI:$IMAGE_TAG

  #     - echo Pushing Tophat Docker image...
  #     - docker push $ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$TOPHAT_REPOSITORY_URI:$IMAGE_TAG

  #     - echo Pushing API Server Docker image...
      # - docker push $ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$API_SERVER_REPOSITORY_URI:$IMAGE_TAG

      # Deploy ECS and API
      # - |
      #   sam deploy -t root.yml \
      #     --config-env ephemeral \
      #     --parameter-overrides \
      #       "pAppName=${APP_NAME} \
      #       pVpcId=vpc-09ef4a53e9290ca17 \
      #       pPrivateSubnetId1=subnet-01086857935bfcf34 \ 
      #       pPrivateSubnetId2=subnet-056e57e04fea05dd5 \
      #       pPublicSubnetId1=subnet-0598f465e77230bd5 \
      #       pPublicSubnetId2=subnet-0d650820a97fa5ba3 \
      #       pR53HostedZoneId=Z0323068C9DQS081P13G, \
      #       pACMCertificateArn=arn:aws:acm:us-east-1:174743933558:certificate/07f48ca4-3dcf-4ed7-b4df-147b3412be62" \
      #     --stack-name ${APP_NAME}-ephemeral \
      #     --tags CleanupDate=$(date -u -d "+10 days" '+%Y-%m-%dT%H:%M:%SZ') \
      #     --on-failure DELETE

 
